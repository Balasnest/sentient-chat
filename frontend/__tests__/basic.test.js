describe('Basic Test', () => {
  test('should work', () => {
    expect(1 + 1).toBe(2);
  });

  test('should handle basic operations', () => {
    const result = 2 * 3;
    expect(result).toBe(6);
  });
}); 